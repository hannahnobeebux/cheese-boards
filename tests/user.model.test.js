//STEP ONE - REQUIRING THE CORRECT FILES TO TEST 
const { User } = require("../models");
const {DataTypes, Model} = require('sequelize')
const {db} = require('../db/db')

//STEP TWO - TESTING 
test ('Table "User" exists after creating it ', async () => {

})